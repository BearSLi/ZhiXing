# 工单系统 · 后端直接依赖（版本钉死，保证环境可复现）
# 生成依据：本机实际安装版本
# 说明：只钉"直接依赖"，传递依赖交给 pip 解析器（避免锁死整棵依赖树）

fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
PyJWT==2.14.0
bcrypt==5.0.0
python-dotenv==1.2.3
openai==3.16.2

# ── 开发/测试用（可选安装，容器镜像不含）──
# pytest==8.0.0
# httpx==0.27.0
